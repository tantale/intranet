<%namespace file="intranet.templates.pointage.order.tasks.assignations.widgets" import="assignations"/>
${assignations(task=task,
               active_employees=active_employees,
               **hidden)}
